"use strict";

var header = document.getElementById("js-header");
var hamburgerIcon = document.getElementById("js-hamburger");
var nav = document.getElementById("js-navigation");
var navHeader = document.getElementById("js-navigation-header"); // Back to top

var backToTop = document.getElementById("back-to-top");

backToTop.onclick = function () {
  document.body.scrollTop = 0;
  document.documentElement.scrollTop = 0;
};

var scroll = function scroll() {
  if (window.pageYOffset > 330) {
    backToTop.style.display = "block";
  } else {
    backToTop.style.display = "none";
  }
};

window.onscroll = function () {
  scroll();
}; // Toggle Hamburger icon when open


function toggleHamburger() {
  if (hamburgerIcon.classList.contains("-open")) {
    hamburgerIcon.classList.toggle("-open");
    nav.classList.toggle("-open");
    navHeader.removeChild(hamburgerIcon);
    header.appendChild(hamburgerIcon);
  } else {
    hamburgerIcon.classList.toggle("-open");
    nav.classList.toggle("-open");
    header.removeChild(hamburgerIcon);
    navHeader.appendChild(hamburgerIcon);
  }
} // Hamburger icon animation toggle.


hamburgerIcon.addEventListener("click", toggleHamburger, true); // Carousel

var tabs = document.querySelectorAll('.carousel__item');
var tabsTitles = document.querySelectorAll('.carousel__label');
tabsTitles.forEach(function (tab, index) {
  tab.addEventListener("click", function () {
    tabsTitles.forEach(function (tab) {
      tab.classList.remove("-active");
    });
    tab.classList.toggle("-active");
    tabs.forEach(function (tab) {
      tab.classList.remove("-active");
    });
    tabs[index].classList.toggle("-active");
  });
}); // FAQ

var items = document.querySelectorAll('.faq__item');
items.forEach(function (item) {
  item.addEventListener('click', function () {
    item.classList.toggle('-active');
  }, true);
});
//# sourceMappingURL=app.min.js.map
